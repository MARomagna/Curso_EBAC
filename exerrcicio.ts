const multiplicar = (base: number, altura: number): number => base * altura;
console.log(multiplicar(5, 8));

const saudacao = (nome: string): string => `Ola ${nome}`;
console.log(saudacao("Marco"));
